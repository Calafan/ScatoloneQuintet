# Card images — not tracked in git. Physical assets, managed locally.
# The repository holds only metadata (CSV) and text docs.
Source/

# Generated view tree - symlinks/hardlinks pointing back into Source/, deleted
# and rebuilt from scratch by `build-views` on every run. Output, not state.
Views/

# OS / editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# IDE
.vs/
.idea/
*.user
*.suo

# Build artifacts
bin/
obj/